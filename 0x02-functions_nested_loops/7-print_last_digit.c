#include "main.h"
#include <math.h>
/**
 * print_last_digit - check the code
 * @l: Last digit
 * Description: Prints last digit of a number
 * Return: returns 0
 */
int print_last_digit(int l)
{
	int r;

	r = l % 10;
	return (r);
}
