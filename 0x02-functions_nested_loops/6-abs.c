#include "main.h"
#include "math.h"
/**
 * _abs - check the code
 * @ : integer argument
 * Description: Computes absolute value
 * Return: returns absolute value
 */
int _abs(int n)
{
	int m;

	m = abs(n);
	return (m);
}
