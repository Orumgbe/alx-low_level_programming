#include "main.h"
#include <math.h>
/**
 * add - check the code
 * @i: first digit to sum
 * @j: second digit to sum
 * Description: Adds two numbers
 * Return: returns sum;
 */
int add(int i, int j)
{
	int sum;

	s = i + j;
	return (s);
}
