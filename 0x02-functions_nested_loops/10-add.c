#include "main.c"
#include <math.h>
/**
 * add - check the code
 * @i: first digit to sum
 * @j: second digit to sum
 * Description: Adds two numbers
 * Return: returns sum;
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
